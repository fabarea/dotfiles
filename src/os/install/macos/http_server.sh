#!/bin/bash

cd "$(dirname "${BASH_SOURCE[0]}")" \
    && . "../../utils.sh" \
    && . "./utils.sh"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

print_in_purple "\n   Image Tools\n\n"

#brew tap denji/nginx;
#brew install nginx-full --with-lua-module --with-set-misc-module --with-http2 --with-gzip-static
# todo test me!
#brew_install "Nginx" "nginx-full --with-lua-module --with-set-misc-module --with-http2 --with-gzip-static" "denji/nginx" "cask"
